let x = Number
let y = Number
let operacao {
    soma = (x + y),
    mult = (x * y),
    subt = (x - y),
    div = (x / y)
}
console.log("O resultado da soma é" + soma)
console.log("O resultado da soma é" + soma)
console.log("O resultado da soma é" + soma)
console.log("O resultado da soma é" + soma)